# docs/

This directory is the **GitHub Pages build output** — generated by Vite from the `landing/` source. Do not edit files here by hand.

To rebuild:

```bash
cd landing
npm run build
```

Vite outputs to `../docs/` so GitHub Pages can serve the site from the `docs/` folder on the `main` branch.
